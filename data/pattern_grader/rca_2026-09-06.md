# Pattern Grader scheduled-task RCA — 2026-09-06

Category: **A — scheduled run never launched**.

The scanner itself is healthy. `python scripts/pattern_grader_scanner.py
--dry-run --print` exited zero, found its source report, and projected 96
detections without writing data.

Windows Task Scheduler reported result `0x800710E0` ("The operator or
administrator has refused the request"). The Task Scheduler Operational log
contained four Event 153 records stating that
`\VibeTrade\PatternGrader-Scanner-Intraday` missed its schedule. The task's XML
has `StartWhenAvailable=false`; the scanner log contains no crash traceback.

The existing task was run once without changing its configuration. Because the
runner has a fail-closed weekend guard, it exited successfully without
fabricating grades. Health changed from `ERROR=1` to `ERROR=0`.

The registration script now exposes an explicit `-StartWhenAvailable` switch.
After the user authorized completion, the installed task was updated to that
setting and rerun successfully (`LastTaskResult=0`). The command, cadence,
runner, and read-only authority were unchanged.

No scanner scoring, output schema, trading logic, or health classification was
changed.
